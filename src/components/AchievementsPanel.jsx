import React,{useMemo}from'react';import{getAchievements,getStreak}from'../data/studentProfile';export default function AchievementsPanel({profile}){const achievements=useMemo(()=>getAchievements(profile),[profile]);const streak=getStreak(profile);return <section className="report achievements-panel"><div className="reportrow"><div><p className="step">REWARDS</p><h3>Achievements & Streak</h3></div><b>🔥 {streak} day{streak===1?'':'s'}</b></div>{achievements.length?<div className="achievement-grid">{achievements.map(a=><div className="achievement-card" key={a.id}><span>{a.icon}</span><b>{a.title}</b><small>{a.description}</small></div>)}</div>:<p>Complete an assessment to unlock your first achievement.</p>}</section>}
