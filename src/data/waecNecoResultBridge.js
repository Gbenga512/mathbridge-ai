import{buildExamReport}from'./examResults';import{saveExamReport}from'./examReportStorage';import{analyseExamPerformance,buildRevisionRecommendations}from'./studentPerformanceAnalytics';
export const saveCbtResultToMathBridge=(exam,questions,answers)=>{const raw={exam,questions,answers};const result=buildExamReport({...raw,...(()=>{let correct=0;const results=questions.map((question,index)=>{const isCorrect=answers[index]===question.answer;if(isCorrect)correct++;return{question,isCorrect,answer:answers[index]}});return{correct,total:questions.length,percentage:questions.length?Math.round(correct/questions.length*100):0,results}})()});const analysis=analyseExamPerformance(questions,answers);const revisionRecommendations=buildRevisionRecommendations(analysis);const finalResult={...result,...analysis,revisionRecommendations,exam,submittedAt:new Date().toISOString()};saveExamReport(finalResult);return finalResult};
