import React,{useState}from'react';
import {saveCloudProgress,getProgress} from './cloudProgress';

export default function StudentAccount({onBack,onContinue}){
 const[mode,setMode]=useState('signup'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[studentName,setStudentName]=useState(''),[message,setMessage]=useState('');
 const submit=async e=>{e.preventDefault();if(!email||!password||(mode==='signup'&&!studentName)){setMessage('Please complete all required fields.');return}const userId=`student:${email.trim().toLowerCase()}`;if(mode==='signup'){await saveCloudProgress(userId,{userId,role:'student',name:studentName,term:'T1',week:1,masteredWeeks:[]});setMessage('Student account created on the MVP storage layer.');}else{const p=await getProgress(userId);setMessage(p?`Welcome back, ${p.name||'student'}! Your saved progress is available.`:'No account record was found on this browser. Production login will use cloud authentication.')} };
 return <main className="dashboard account-page"><div className="dashhead"><div><p className="step">🎓 STUDENT ACCOUNT</p><h2>{mode==='signup'?'Create your MathBridge account':'Welcome back'}</h2><p className="leveltext">{mode==='signup'?'Save your Maths journey and continue across devices once cloud authentication is connected.':'Continue your saved Maths journey.'}</p></div><button className="secondary" onClick={onBack}>← Back</button></div><div className="account-card"><div className="account-tabs"><button className={mode==='signup'?'active':''} onClick={()=>{setMode('signup');setMessage('')}}>Student Signup</button><button className={mode==='login'?'active':''} onClick={()=>{setMode('login');setMessage('')}}>Student Login</button></div><form onSubmit={submit}>{mode==='signup'&&<input value={studentName} onChange={e=>setStudentName(e.target.value)} placeholder="Student name"/>}<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" type="submit">{mode==='signup'?'Create Student Account →':'Login →'}</button></form>{message&&<div className="recommend"><b>{message}</b><small>Current MVP uses browser storage. Do not use this prototype for sensitive production credentials.</small></div>}<button className="secondary full" onClick={onContinue}>Continue to Maths →</button></div></main>
}
