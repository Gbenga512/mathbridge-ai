export const getExamGrade=p=>p>=75?'A1':p>=70?'B2':p>=65?'B3':p>=60?'C4':p>=55?'C5':p>=50?'C6':p>=45?'D7':p>=40?'E8':'F9';
export const buildExamReport=(result)=>{const byTopic={};result.results.forEach(r=>{const t=r.question.topic||'General';byTopic[t]??={correct:0,total:0};byTopic[t].total++;if(r.isCorrect)byTopic[t].correct++});const topicPerformance=Object.fromEntries(Object.entries(byTopic).map(([t,v])=>[t,{...v,percentage:Math.round(v.correct/v.total*100)}]));const weakTopics=Object.entries(topicPerformance).filter(([,v])=>v.percentage<60).map(([t])=>t);return{...result,grade:getExamGrade(result.percentage),topicPerformance,weakTopics,passed:result.percentage>=40}};
