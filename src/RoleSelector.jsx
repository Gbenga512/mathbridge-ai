import React,{useState}from'react';import{ROLES,roleLabels,saveRole}from'./authRoles';
export default function RoleSelector({onSelect}){const[role,setRole]=useState(ROLES.student);return <section className="role-selector"><p className="step">WHO ARE YOU?</p><h2>Choose your MathBridge experience</h2><div className="role-grid">{Object.values(ROLES).map(r=><button key={r} className={`role-card ${role===r?'active':''}`} onClick={()=>setRole(r)}><span>{r==='student'?'🎓':r==='parent'?'👨‍👩‍👧':'👨‍🏫'}</span><b>{roleLabels[r]}</b><small>{r==='student'?'Learn, practise and master Maths.':r==='parent'?'Monitor your child’s progress.':'Monitor your class and target support.'}</small></button>)}</div><button className="primary" onClick={()=>{saveRole(role);onSelect(role)}}>Continue as {roleLabels[role]} →</button><p className="note">MVP role selection. Secure account authentication will be added before public launch.</p></section>}
