export const buildJSS3MasteryProfile=(history=[],topics=[])=>{const latest=history[0]?.topics||[];const map=new Map(latest.map(x=>[x.topic,x]));return topics.map(topic=>{const x=map.get(topic)||{topic,percentage:0};const percentage=x.percentage||0;return{topic,percentage,status:percentage>=80?'Mastered':percentage>=60?'Developing':'Needs Practice',remaining:Math.max(0,80-percentage),recommendation:percentage>=80?'Maintain with mixed practice':percentage>=60?'Practise until 80%':'Review the lesson, then practise again'}})};
